import types from './actionTypes';
let defaultState = {

};
export default (state=defaultState,action) => {
    switch(action.type){
        case '': {

        }
        default: return state;
    }
}